# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the develop branch
  push:
    branches:
      - main
      - develop
      - ch-setup-ci-github-actions-cp-27
  pull_request:
    branches:
      - main
      - develop
      - ch-setup-ci-github-actions-cp-27

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    concurrency: test_environment
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      # setup the node development environment
      - uses: actions/setup-node@v2
        with:
          node-version: "16.x"
      

      # Runs a single command using the runners shell
      - name: Install all dependencies
        run: npm install

      # Runs a set of commands using the runners shell
      # to added after the merge of test PR
<<<<<<< HEAD
      - name: Run a test and generate coverage
        run: npm run coverage
      - name: Genrating coveralls report
        uses: coverallsapp/github-action@master
        with: 
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish to codeclimate
        uses: paambaati/codeclimate-action@v2.5.7
        env:
          CC_TEST_REPORTER_ID: ${{ secrets.CC_TEST_REPORTER_ID}}
=======
      # - name: Run a tests
      #   run: npm run test --if-exist
>>>>>>> 8df2190 (ch-(setup-ci)-remove non-existent test script)
  deploy:
    # deploy to heroku dev pipeline
    concurrency: heroku_deployment
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "phantom-codebandits-staging"
          heroku_email: "tumbafabruce@gmail.com"
          
